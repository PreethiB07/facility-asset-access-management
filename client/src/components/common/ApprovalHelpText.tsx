export default function ApprovalHelpText() {
  return (
    <p className="field-hint approval-help">
      If enabled, access requests require manager approval. If disabled, eligible access requests
      may be automatically approved.
    </p>
  );
}
